{
  "name": "quizarena",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:web": "pnpm --filter @quizarena/web dev",
    "dev:server": "pnpm --filter @quizarena/server dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=18"
  }
}
